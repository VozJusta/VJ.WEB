version: 2
updates:
  # Mantém os SHAs fixados das GitHub Actions atualizados via PRs revisados.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
